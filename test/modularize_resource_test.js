/**
 * Test case for modularizeResource.
 * Runs with mocha.
 */
'use strict'

const modularizeResource = require('../lib/resourceful/modularize_resource.js')
const assert = require('assert')
const co = require('co')

describe('modularize-resource', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Modularize resource', () => co(function * () {

  }))
})

/* global describe, before, after, it */
